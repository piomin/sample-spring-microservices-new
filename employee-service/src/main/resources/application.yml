spring:
  application:
    name: employee
  data:
    mongodb:
      uri: mongodb://mongodb/${MONGO_DATABASE}
      password: ${MONGO_PASSWORD}
      username: ${MONGO_USERNAME}
      
logging:
  pattern: 
    console: "%d{yyyy-MM-dd HH:mm:ss} ${LOG_LEVEL_PATTERN:-%5p} %m%n"